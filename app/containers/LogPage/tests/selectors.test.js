// import { fromJS } from 'immutable';
// import { makeSelectLogPageDomain } from '../selectors';

// const selector = makeSelectLogPageDomain();

describe('makeSelectLogPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
