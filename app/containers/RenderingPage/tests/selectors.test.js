// import { fromJS } from 'immutable';
// import { makeSelectRenderingPageDomain } from '../selectors';

// const selector = makeSelectRenderingPageDomain();

describe('makeSelectRenderingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
